name: Regression Tests Execution

on:
  workflow_run:
    workflows: ["Smoke Tests Execution"]
    types:
      - completed
  workflow_dispatch:
    inputs:
      env:
        description: 'Test environment'
        default: 'qa'
      browser:
        description: 'Browser to run tests'
        default: 'chromium'
      headless:
        description: 'Headless mode'
        default: 'true'
      threadcount:
        description: 'Parallel threads'
        default: '1'
      tag:
        description: 'Cucumber tags'
        default: '@regression'

jobs:
  regression-job:
    runs-on: ubuntu-latest
    # FIX: explicit conclusion check prevents regression
    # running when smoke fails or is cancelled
    if: >
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'workflow_run' &&
       github.event.workflow_run.conclusion == 'success')

    steps:
      # ─────────────────────────────────────────
      # 0️⃣  Guard — exit immediately if smoke failed
      #      Reliable fix for GitHub's workflow_run
      #      conclusion check quirk
      # ─────────────────────────────────────────
      - name: Check Smoke Conclusion
        id: guard
        run: |
          CONCLUSION="${{ github.event.workflow_run.conclusion }}"
          TRIGGER_EVENT="${{ github.event.workflow_run.event }}"
          echo "Smoke workflow conclusion: ${CONCLUSION}"
          echo "Smoke was triggered by: ${TRIGGER_EVENT}"

          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            if [ "${TRIGGER_EVENT}" = "workflow_dispatch" ]; then
              echo "ℹ️  Smoke was manually triggered — regression will not auto-run"
              echo "proceed=false" >> $GITHUB_OUTPUT
            elif [ "${CONCLUSION}" != "success" ]; then
              echo "❌ Smoke did not pass (conclusion: ${CONCLUSION}) — skipping regression"
              echo "proceed=false" >> $GITHUB_OUTPUT
            else
              echo "✅ Smoke passed via push — proceeding with regression"
              echo "proceed=true" >> $GITHUB_OUTPUT
            fi
          else
            echo "✅ Manual regression trigger — proceeding"
            echo "proceed=true" >> $GITHUB_OUTPUT
          fi

      # ─────────────────────────────────────────
      # 1️⃣  Checkout main branch
      # ─────────────────────────────────────────
      - name: Checkout Main
        if: steps.guard.outputs.proceed == 'true'
        uses: actions/checkout@v4

      # ─────────────────────────────────────────
      # 2️⃣  Setup Java 21
      # ─────────────────────────────────────────
      - name: Setup Java
        if: steps.guard.outputs.proceed == 'true'
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      # ─────────────────────────────────────────
      # 3️⃣  Install Allure CLI
      # ─────────────────────────────────────────
      - name: Install Allure CLI
        if: steps.guard.outputs.proceed == 'true'
        run: npm install -g allure-commandline

      # ─────────────────────────────────────────
      # 4️⃣  Install Playwright browsers via Java CLI
      # ─────────────────────────────────────────
      - name: Install Playwright Browsers
        if: steps.guard.outputs.proceed == 'true'
        run: mvn exec:java -e -D exec.mainClass=com.microsoft.playwright.CLI -D exec.args="install --with-deps" -D exec.classpathScope=test

      # ─────────────────────────────────────────
      # 5️⃣  Restore shared history from gh-pages
      # ─────────────────────────────────────────
      - name: Checkout gh-pages for History
        if: steps.guard.outputs.proceed == 'true'
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages
        continue-on-error: true

      # ─────────────────────────────────────────
      # 6️⃣  Resolve build number
      #      Auto-triggered: pair with smoke build number
      #      Manual: use own run number
      # ─────────────────────────────────────────
      - name: Resolve Build Number
        if: steps.guard.outputs.proceed == 'true'
        id: build
        run: |
          if [ "${{ github.event_name }}" = "workflow_run" ]; then
            echo "number=${{ github.event.workflow_run.run_number }}" >> $GITHUB_OUTPUT
            echo "ℹ️  Auto-triggered — using smoke build number: ${{ github.event.workflow_run.run_number }}"
          else
            echo "number=${{ github.run_number }}" >> $GITHUB_OUTPUT
            echo "ℹ️  Manual trigger — using own run number: ${{ github.run_number }}"
          fi

      # ─────────────────────────────────────────
      # 7️⃣  Run Regression Tests
      # ─────────────────────────────────────────
      - name: Run Regression Tests
        if: steps.guard.outputs.proceed == 'true'
        id: run
        continue-on-error: true
        run: |
          xvfb-run --auto-servernum -- mvn clean test \
            -Dtestng.suite.file=testng-regression.xml \
            -Dcucumber.filter.tags="${{ github.event.inputs.tag || '@regression' }}" \
            -Denv="${{ github.event.inputs.env || 'qa' }}" \
            -Dbrowser="${{ github.event.inputs.browser || 'chromium' }}" \
            -Dheadless="${{ github.event.inputs.headless || 'true' }}" \
            -Dparallel.thread.count="${{ github.event.inputs.threadcount || '1' }}" \
            -Dallure.results.directory=target/allure-results

      - name: Restore Shared History into allure-results
        if: steps.guard.outputs.proceed == 'true'
        run: |
          mkdir -p target/allure-results/history
          if [ -d gh-pages/allure-history/shared/history ]; then
            echo "✅ Shared history found — restoring after test run"
            cp -r gh-pages/allure-history/shared/history/. target/allure-results/history/
          else
            echo "ℹ️  No shared history yet — first run"
          fi

      # ─────────────────────────────────────────
      # 8️⃣  Debug — verify outputs
      #      Remove after first confirmed clean run
      # ─────────────────────────────────────────
      - name: Debug — Verify Output Paths
        if: steps.guard.outputs.proceed == 'true'
        run: |
          echo "====== target/allure-results ======"
          ls -la target/allure-results/ || echo "❌ NOT FOUND"
          echo "====== history ======"
          ls -la target/allure-results/history/ || echo "❌ history empty or missing"
          echo "====== environment.properties ======"
          cat target/allure-results/environment.properties || echo "❌ NOT FOUND"
          echo "====== test-output ======"
          find . -path ./gh-pages -prune -o -name "failedLocators_*.json" -print
          echo "====== defect-age-report ======"
          find . -path ./gh-pages -prune -o -name "defect-age-report.csv" -print

      # ─────────────────────────────────────────
      # 9️⃣  Generate Defect Age Report
      # ─────────────────────────────────────────
      - name: Generate Defect Age Report
        if: steps.guard.outputs.proceed == 'true'
        run: |
          mvn exec:java \
            -Dexec.mainClass="com.samtech.qa.testutilities.AllureDefectAge" \
            -Dexec.classpathScope=test \
            -Denv="${{ github.event.inputs.env || 'qa' }}" \
            -Dbrowser="${{ github.event.inputs.browser || 'chromium' }}" \
            -Dheadless="${{ github.event.inputs.headless || 'true' }}"
          echo "====== defect-age-report.csv ======"
          cat target/defect-age-report.csv || echo "❌ not generated"

      # ─────────────────────────────────────────
      # 🔟  Generate Allure HTML Report
      # ─────────────────────────────────────────
      - name: Generate Allure Report
        if: steps.guard.outputs.proceed == 'true'
        run: |
          allure generate target/allure-results -o allure-report-regression --clean
          echo "====== allure-report-regression/history ======"
          ls allure-report-regression/history/ || echo "❌ history missing"

      # ─────────────────────────────────────────
      # 1️⃣1️⃣  Save history + versioned report + index
      # ─────────────────────────────────────────
      - name: Update gh-pages — History + Versioned Report + Index
        if: steps.guard.outputs.proceed == 'true'
        run: |
          BUILD=${{ steps.build.outputs.number }}
          REPO_NAME=$(echo '${{ github.repository }}' | cut -d'/' -f2)
          REPO_URL="https://${{ github.repository_owner }}.github.io/${REPO_NAME}"
          MAX_BUILDS=20

          # ── Clone gh-pages ──
          rm -rf gh-pages-push
          git clone --branch gh-pages --single-branch \
            https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git \
            gh-pages-push

          cd gh-pages-push

          # ── Save shared history ──
          mkdir -p allure-history/shared
          rm -rf allure-history/shared/history
          cp -r ../allure-report-regression/history allure-history/shared/history

          # ── Save versioned regression report ──
          mkdir -p regression-report/build-${BUILD}
          cp -r ../allure-report-regression/. regression-report/build-${BUILD}/

          # ── Write build metadata ──
          mkdir -p build-meta
          echo "${{ steps.run.outcome }}" > build-meta/regression-${BUILD}.txt

          # ── Cleanup old regression builds ──
          cd regression-report
          ls -d build-* 2>/dev/null | sort -t- -k2 -n | head -n -${MAX_BUILDS} | xargs rm -rf 2>/dev/null || true
          cd ..

          # ── Generate unified index page ──
          cat > index.html << 'HTMLEOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8"/>
            <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
            <title>Test Reports Dashboard</title>
            <style>
              * { box-sizing: border-box; margin: 0; padding: 0; }
              body { font-family: Arial, sans-serif; background: #f5f5f5; }
              header { background: #2c3e50; color: white; padding: 20px 40px; }
              header h1 { font-size: 22px; }
              header p { margin-top: 4px; opacity: 0.7; font-size: 12px; }
              .container { max-width: 1100px; margin: 30px auto; padding: 0 20px; }
              table { width: 100%; border-collapse: collapse; background: white;
                      border-radius: 8px; overflow: hidden;
                      box-shadow: 0 2px 8px rgba(0,0,0,0.1); }
              th { background: #2c3e50; color: white; padding: 14px 16px;
                   text-align: left; font-size: 13px; }
              td { padding: 12px 16px; border-bottom: 1px solid #eee; font-size: 13px; }
              tr:last-child td { border-bottom: none; }
              tr:hover td { background: #f9f9f9; }
              .badge { display: inline-block; padding: 3px 10px; border-radius: 12px;
                       font-size: 11px; font-weight: bold; }
              .pass { background: #d4edda; color: #155724; }
              .fail { background: #f8d7da; color: #721c24; }
              .pending { background: #fff3cd; color: #856404; }
              a { color: #2980b9; text-decoration: none; font-weight: 500; }
              a:hover { text-decoration: underline; }
              .na { color: #aaa; font-style: italic; }
            </style>
          </head>
          <body>
            <header>
              <h1>🧪 Test Reports Dashboard</h1>
              <p>Smoke &amp; Regression — Last 20 Builds</p>
            </header>
            <div class="container">
              <table>
                <thead>
                  <tr>
                    <th>Build</th>
                    <th>Smoke Status</th>
                    <th>Smoke Report</th>
                    <th>Regression Status</th>
                    <th>Regression Report</th>
                  </tr>
                </thead>
                <tbody id="tbody">
                  <tr><td colspan="5" id="loading">Loading builds...</td></tr>
                </tbody>
              </table>
            </div>
            <script>
              const repoUrl = "REPO_URL_PLACEHOLDER";
              async function safeFetch(url) {
                try {
                  const r = await fetch(url);
                  if (!r.ok) return null;
                  return (await r.text()).trim();
                } catch { return null; }
              }
              async function loadBuilds() {
                const tbody = document.getElementById("tbody");
                const checks = Array.from({length: 200}, (_, i) => i + 1);
                const smokeResults = await Promise.all(
                  checks.map(n => safeFetch(`${repoUrl}/build-meta/smoke-${n}.txt`).then(v => v !== null ? n : null))
                );
                const regressionResults = await Promise.all(
                  checks.map(n => safeFetch(`${repoUrl}/build-meta/regression-${n}.txt`).then(v => v !== null ? n : null))
                );
                const smokeBuilds = smokeResults.filter(n => n !== null);
                const regressionOnly = regressionResults.filter(n => n !== null && !smokeBuilds.includes(n));
                const allBuilds = [...new Set([...smokeBuilds, ...regressionOnly])].sort((a,b) => b-a).slice(0,20);
                if (allBuilds.length === 0) {
                  tbody.innerHTML = '<tr><td colspan="5" style="text-align:center;color:#aaa;padding:30px">No builds found yet</td></tr>';
                  return;
                }
                tbody.innerHTML = "";
                for (const build of allBuilds) {
                  const smokeStatus = await safeFetch(`${repoUrl}/build-meta/smoke-${build}.txt`);
                  const regressionStatus = await safeFetch(`${repoUrl}/build-meta/regression-${build}.txt`);
                  const triggerType = await safeFetch(`${repoUrl}/build-meta/smoke-trigger-${build}.txt`);
                  const smokeBadge = smokeStatus === null
                    ? '<span class="na">—</span>'
                    : smokeStatus === 'success'
                    ? '<span class="badge pass">✅ PASSED</span>'
                    : '<span class="badge fail">❌ FAILED</span>';
                  const regressionBadge = regressionStatus === null
                    ? (triggerType === 'manual' ? '<span class="na">Manual smoke — regression skipped</span>' : '<span class="na">Smoke failed — regression skipped</span>')
                    : regressionStatus === 'success'
                    ? '<span class="badge pass">✅ PASSED</span>'
                    : '<span class="badge fail">❌ FAILED</span>';
                  const smokeLink = smokeStatus === null
                    ? '<span class="na">—</span>'
                    : `<a href="${repoUrl}/smoke-report/build-${build}/index.html" target="_blank">View Report</a>`;
                  const regressionLink = regressionStatus === null
                    ? '<span class="na">—</span>'
                    : `<a href="${repoUrl}/regression-report/build-${build}/index.html" target="_blank">View Report</a>`;
                  tbody.innerHTML += `<tr>
                    <td><strong>#${build}</strong></td>
                    <td>${smokeBadge}</td>
                    <td>${smokeLink}</td>
                    <td>${regressionBadge}</td>
                    <td>${regressionLink}</td>
                  </tr>`;
                }
              }
              loadBuilds();
            </script>
          </body>
          </html>
          HTMLEOF

          sed -i "s|REPO_URL_PLACEHOLDER|${REPO_URL}|g" index.html

          # ── Commit and push ──
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git diff --cached --quiet \
            && echo "ℹ️  No changes to commit" \
            || git commit -m "chore(regression): build #${BUILD} [skip ci]"
          git pull --rebase origin gh-pages || true
          git push origin gh-pages
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # ─────────────────────────────────────────
      # 1️⃣2️⃣  Upload artifacts
      # ─────────────────────────────────────────
      - name: Upload Regression Artifacts
        if: steps.guard.outputs.proceed == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: regression-artifacts-build-${{ steps.build.outputs.number }}
          if-no-files-found: warn
          retention-days: 30
          path: |
            target/allure-results/
            allure-report-regression/
            target/defect-age-report.csv
            test-output/failedLocators_*.json
            test-output/logs/

      # ─────────────────────────────────────────
      # 1️⃣3️⃣  Report outcome — non-blocking
      # ─────────────────────────────────────────
      - name: Report Regression Outcome
        if: steps.guard.outputs.proceed == 'true'
        run: |
          if [ "${{ steps.run.outcome }}" = "failure" ]; then
            echo "⚠️  Regression tests FAILED — check the report"
            echo "ℹ️  Pipeline continues — regression is non-blocking"
          else
            echo "✅ All regression tests passed"
          fi