allure.results.directory=target/allure-results
# This ensures Allure uses the Scenario Name + Parameters to group retries
allure.link.tms.pattern={}
allure.link.issue.pattern={}
# This ensures each retry gets a unique file name
allure.results.indent=true
# This is the critical setting for Maven/Surefire environments
allure.tests.management.enabled=false
